Perform a full, critical, automated audit of the entire project source.

🔍 Search for ALL style-related code patterns, including:
	•	color, background, background-color, fill, stroke
	•	Tailwind classes: bg-, text-, border-, shadow-, rounded-, ring-, tracking-, p-, m-, gap-
	•	Direct values: rgba, rgb, # hex codes (#[0-9a-fA-F]{3,6})
	•	Typography: font-family, font-weight, font-size, line-height, letter-spacing
	•	Shadows, spacing: box-shadow, border-radius, padding, margin
	•	Animation: transition, ease, duration
	•	Inline styles: style={{...}}, style=
	•	CSS blocks: css\`` (for styled components) or .css` files

⸻

🔧 Enforce corrections

✅ Replace or refactor ALL these direct styles to reference:
	•	design-system/tokens.ts for typography, color, radius, spacing, shadows
	•	tailwind.config.ts theme extensions for colors, fonts, spacing

✅ Remove any hard-coded hex, rgba, or direct CSS values.
✅ Replace all inline styles and legacy scoped CSS with tokenized or Tailwind utility-based styling.

⸻

🌗 Ensure consistent theme

✅ Dark/light mode must function flawlessly across the entire app:
	•	Sidebar, cards, tables, wizards, dashboard charts
	•	No missing theme switches, no hard-coded colors that break in dark mode

✅ Typography must be consistently:
	•	Cormorant Garamond for headings
	•	Inter for body

✅ Brand colors must be consistently applied:
	•	Purple #7A51E1 for primary actions, progress, accents
	•	Gold #E3C76F for secondary accents
	•	No off-brand or fallback default colors

✅ Hover, focus, and active states must show elegant luxury transitions as documented.

✅ Spacing must follow your design scale (4/8/12/16/24px).

✅ Accessibility: ensure WCAG AA contrast for all text, buttons, interactive elements.

✅ Responsive: sidebar collapses, tap-friendly buttons, text scales correctly.

⸻

🗂 Also audit structure

✅ Check project structure for:
	•	Duplicate or dead code files
	•	Misplaced components
	•	Broken imports or circular dependencies
	•	Clean up and organize as needed

⸻
